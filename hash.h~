#ifndef HASH_H
#define HASH_H

#include"list.h"

class hash
{
 public:
  hash();
  ~hash();
  int add(student kid);
  void display();
  
 private:
  int hf(int key);
  list* array;
  int size;
};
  

#endif
